# ThingZ API 接口文档

## 基础信息

**生产环境**: `https://api.anyongtech.cn/thingz/`

**认证方式**: Bearer Token (在请求头中添加 `Authorization: Bearer {token}`)

---

## 目录

- [1. 静态页面](#1-静态页面)
- [2. 用户管理](#2-用户管理)
- [3. 小屋管理](#3-小屋管理)
- [4. 容器管理](#4-容器管理)
- [5. 物品管理](#5-物品管理)
- [6. 存储管理](#6-存储管理)

---

## 1. 静态页面

### 1.1 访问应用官网主页

**接口路径**: `GET /v1/static/index`

**接口描述**: 提供ThingZ应用的官网主页,包含应用介绍和下载链接

**请求参数**: 无

**返回格式**: HTML

---

### 1.2 访问隐私政策页面

**接口路径**: `GET /v1/static/privacy-policy`

**接口描述**: 提供ThingZ应用的隐私政策页面

**请求参数**: 无

**返回格式**: HTML

---

## 2. 用户管理

### 2.1 用户注册

**接口路径**: `POST /api/user/register`

**接口描述**: 用户注册接口

**请求体**: `RegisterRequest`

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    // 用户信息
  }
}
```

---

### 2.2 用户登录

**接口路径**: `POST /api/user/login`

**接口描述**: 用户名密码登录

**请求体**: `LoginRequest`
```json
{
  "username": "string",
  "password": "string"
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "string",
    "refreshToken": "string"
  }
}
```

---

### 2.3 短信验证码登录

**接口路径**: `POST /api/user/sms-login`

**接口描述**: 使用手机号和短信验证码登录

**请求体**: `SmsLoginRequest`
```json
{
  "phone": "string",
  "code": "string"
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "string",
    "refreshToken": "string"
  }
}
```

---

### 2.4 刷新令牌

**接口路径**: `POST /api/user/refresh-token`

**接口描述**: 刷新访问令牌

**请求参数**: 无

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "string",
    "refreshToken": "string"
  }
}
```

---

### 2.5 修改密码

**接口路径**: `POST /api/user/change-password-v2`

**接口描述**: 修改用户密码

**请求体**: `ChangePasswordRequest`
```json
{
  "oldPassword": "string",
  "newPassword": "string"
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 2.6 重置密码

**接口路径**: `POST /api/user/reset-password`

**接口描述**: 忘记密码后重置

**请求体**: `ResetPasswordRequest`

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 2.7 修改用户资料

**接口路径**: `POST /api/user/change-profile`

**接口描述**: 修改用户基本资料

**请求体**: `ChangeProfileRequest`

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 2.8 设置用户权限

**接口路径**: `POST /api/user/set-auth`

**接口描述**: 设置用户认证权限

**请求体**: `SetAuthRequest`

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 2.9 发送短信验证码

**接口路径**: `POST /api/user/send-sms-code`

**接口描述**: 发送短信验证码到手机

**请求体**: `SmsSendRequest`
```json
{
  "phone": "string"
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 2.10 获取当前用户信息

**接口路径**: `GET /api/user/info`

**接口描述**: 获取当前登录用户的详细信息

**请求参数**: 无

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": "string",
    "username": "string",
    "email": "string",
    "phone": "string",
    "avatar": "string",
    "nickname": "string"
  }
}
```

---

## 3. 小屋管理

### 3.1 获取小屋列表

**接口路径**: `GET /v1/cabins`

**接口描述**: 获取当前用户加入的所有小屋

**请求参数**: 无

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "string",
      "name": "string",
      "description": "string",
      "createdAt": "string"
    }
  ]
}
```

---

### 3.2 创建小屋

**接口路径**: `POST /v1/cabins`

**接口描述**: 创建新的小屋

**请求体**: `CreateCabinRequest`
```json
{
  "name": "string",
  "description": "string"
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "string",
    "name": "string",
    "description": "string"
  }
}
```

---

### 3.3 获取小屋详情

**接口路径**: `GET /v1/cabins/{id}`

**接口描述**: 根据ID获取小屋详细信息

**路径参数**:
- `id`: 小屋ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "string",
    "name": "string",
    "description": "string",
    "members": []
  }
}
```

---

### 3.4 更新小屋信息

**接口路径**: `POST /v1/cabins/modify/{id}`

**接口描述**: 更新指定小屋的信息

**路径参数**:
- `id`: 小屋ID

**请求体**: `UpdateCabinRequest`
```json
{
  "name": "string",
  "description": "string"
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "string",
    "name": "string",
    "description": "string"
  }
}
```

---

### 3.5 删除小屋

**接口路径**: `POST /v1/cabins/remove/{id}`

**接口描述**: 删除指定的小屋

**路径参数**:
- `id`: 小屋ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 3.6 邀请成员

**接口路径**: `POST /v1/cabins/{id}/invite`

**接口描述**: 邀请新成员加入小屋

**路径参数**:
- `id`: 小屋ID

**请求体**: `InviteMemberRequest`

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "invitationId": "string",
    "invitationCode": "string"
  }
}
```

---

### 3.7 加入小屋

**接口路径**: `POST /v1/cabins/{id}/join`

**接口描述**: 通过邀请码加入小屋

**路径参数**:
- `id`: 小屋ID

**请求体**: `JoinCabinRequest`
```json
{
  "invitationCode": "string"
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "cabinId": "string",
    "cabinName": "string"
  }
}
```

---

### 3.8 移除成员

**接口路径**: `POST /v1/cabins/{id}/remove-member/{userId}`

**接口描述**: 从小屋中移除成员

**路径参数**:
- `id`: 小屋ID
- `userId`: 成员ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 3.9 更新成员权限

**接口路径**: `POST /v1/cabins/{id}/change-permission/{userId}`

**接口描述**: 更新小屋成员的权限

**路径参数**:
- `id`: 小屋ID
- `userId`: 成员ID

**请求体**: `UpdateMemberPermissionRequest`

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 3.10 获取小屋成员列表

**接口路径**: `GET /v1/cabins/{id}/members`

**接口描述**: 获取指定小屋的所有成员

**路径参数**:
- `id`: 小屋ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "userId": "string",
      "username": "string",
      "role": "string",
      "joinedAt": "string"
    }
  ]
}
```

---

### 3.11 获取待处理邀请

**接口路径**: `GET /v1/cabins/invitations/pending`

**接口描述**: 获取当前用户的待处理邀请列表

**请求参数**: 无

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "invitationId": "string",
      "cabinId": "string",
      "cabinName": "string",
      "inviterName": "string",
      "createdAt": "string"
    }
  ]
}
```

---

## 4. 容器管理

### 4.1 获取容器列表

**接口路径**: `GET /v1/containers`

**接口描述**: 获取当前用户的所有容器

**请求参数**: 无

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "string",
      "name": "string",
      "location": "string",
      "description": "string",
      "category": "string"
    }
  ]
}
```

---

### 4.2 创建容器

**接口路径**: `POST /v1/containers`

**接口描述**: 创建新的容器

**请求体**: `CreateContainerRequestDTO`
```json
{
  "name": "string",
  "location": "string",
  "description": "string",
  "category": "string",
  "isExpirationReminder": true,
  "room": "string",
  "floor": "string",
  "status": "string",
  "imageUrl": "string"
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "string",
    "name": "string",
    "location": "string"
  }
}
```

---

### 4.3 获取容器详情

**接口路径**: `GET /v1/containers/{id}`

**接口描述**: 根据ID获取容器详细信息

**路径参数**:
- `id`: 容器ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "string",
    "name": "string",
    "location": "string",
    "description": "string",
    "category": "string",
    "itemCount": 0
  }
}
```

---

### 4.4 更新容器信息

**接口路径**: `POST /v1/containers/modify/{id}`

**接口描述**: 更新指定容器的信息

**路径参数**:
- `id`: 容器ID

**请求体**: `UpdateContainerRequestDTO`
```json
{
  "name": "string",
  "location": "string",
  "description": "string",
  "isExpirationReminder": true,
  "floor": "string",
  "room": "string",
  "imageUrl": "string",
  "isShareable": true
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "string",
    "name": "string"
  }
}
```

---

### 4.5 删除容器

**接口路径**: `POST /v1/containers/remove/{id}`

**接口描述**: 删除指定的容器

**路径参数**:
- `id`: 容器ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 4.6 获取容器内物品

**接口路径**: `GET /v1/containers/{id}/items`

**接口描述**: 获取指定容器内的所有物品

**路径参数**:
- `id`: 容器ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "string",
      "name": "string",
      "category": "string"
    }
  ]
}
```

---

### 4.7 生成二维码

**接口路径**: `POST /v1/containers/{id}/qrcode`

**接口描述**: 为容器生成二维码

**路径参数**:
- `id`: 容器ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": "qrcode_url"
}
```

---

### 4.8 绑定NFC标签

**接口路径**: `POST /v1/containers/{id}/nfc`

**接口描述**: 为容器绑定NFC标签

**路径参数**:
- `id`: 容器ID

**查询参数**:
- `nfcTagId`: NFC标签ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": "绑定成功"
}
```

---

## 5. 物品管理

### 5.1 创建物品

**接口路径**: `POST /v1/items`

**接口描述**: 添加新的物品到容器中

**请求体**: `ItemCreateRequest`
```json
{
  "name": "string",
  "containerId": "string",
  "category": "string",
  "quantity": 0,
  "description": "string",
  "expirationDate": "2024-12-31",
  "imageUrl": "string"
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "string",
    "name": "string",
    "containerId": "string"
  }
}
```

---

### 5.2 删除物品

**接口路径**: `POST /v1/items/remove/{id}`

**接口描述**: 根据ID删除物品

**路径参数**:
- `id`: 物品ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 5.3 批量删除物品

**接口路径**: `POST /v1/items/batch/remove`

**接口描述**: 根据ID列表批量删除物品

**请求体**:
```json
["id1", "id2", "id3"]
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 5.4 更新物品

**接口路径**: `POST /v1/items/modify/{id}`

**接口描述**: 根据ID更新物品信息

**路径参数**:
- `id`: 物品ID

**请求体**: `ItemUpdateRequest`
```json
{
  "name": "string",
  "category": "string",
  "quantity": 0,
  "description": "string",
  "expirationDate": "2024-12-31",
  "imageUrl": "string",
  "status": "string"
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "string",
    "name": "string"
  }
}
```

---

### 5.5 获取物品详情

**接口路径**: `GET /v1/items/{id}`

**接口描述**: 根据ID获取物品详细信息

**路径参数**:
- `id`: 物品ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "string",
    "name": "string",
    "containerId": "string",
    "category": "string",
    "quantity": 0,
    "status": "string",
    "expirationDate": "2024-12-31"
  }
}
```

---

### 5.6 查询物品列表

**接口路径**: `GET /v1/items`

**接口描述**: 分页查询物品列表,支持多种条件筛选

**查询参数**:
- `pageNum`: 页码 (默认: 1)
- `pageSize`: 每页大小 (默认: 10)
- `name`: 物品名称 (可选)
- `category`: 分类 (可选)
- `status`: 状态 (可选)
- `containerId`: 容器ID (可选)

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "list": [
      {
        "id": "string",
        "name": "string",
        "category": "string"
      }
    ],
    "pageNum": 1,
    "pageSize": 10
  }
}
```

---

### 5.7 根据容器ID查询物品

**接口路径**: `GET /v1/items/container/{containerId}`

**接口描述**: 获取指定容器中的所有物品

**路径参数**:
- `containerId`: 容器ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "string",
      "name": "string",
      "category": "string"
    }
  ]
}
```

---

### 5.8 根据状态查询物品

**接口路径**: `GET /v1/items/status/{status}`

**接口描述**: 获取指定状态的所有物品

**路径参数**:
- `status`: 物品状态

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "string",
      "name": "string",
      "status": "string"
    }
  ]
}
```

---

### 5.9 查询即将过期的物品

**接口路径**: `GET /v1/items/near-expiration`

**接口描述**: 获取指定天数内即将过期的物品

**查询参数**:
- `days`: 天数 (默认: 7)

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "string",
      "name": "string",
      "expirationDate": "2024-12-31"
    }
  ]
}
```

---

### 5.10 查询已过期的物品

**接口路径**: `GET /v1/items/expired`

**接口描述**: 获取所有已过期的物品

**请求参数**: 无

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "string",
      "name": "string",
      "expirationDate": "2024-12-31"
    }
  ]
}
```

---

### 5.11 根据分类查询物品

**接口路径**: `GET /v1/items/category/{category}`

**接口描述**: 获取指定分类的所有物品

**路径参数**:
- `category`: 物品分类

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "string",
      "name": "string",
      "category": "string"
    }
  ]
}
```

---

### 5.12 批量更新物品状态

**接口路径**: `POST /v1/items/batch/change-status`

**接口描述**: 批量更新多个物品的状态

**请求体**:
```json
["id1", "id2", "id3"]
```

**查询参数**:
- `status`: 新状态

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 5.13 移动物品到容器

**接口路径**: `POST /v1/items/{itemId}/move-to-container`

**接口描述**: 将物品移动到指定容器

**路径参数**:
- `itemId`: 物品ID

**查询参数**:
- `containerId`: 目标容器ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 5.14 取出物品

**接口路径**: `POST /v1/items/{itemId}/take-out-item`

**接口描述**: 将物品从容器中取出

**路径参数**:
- `itemId`: 物品ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 5.15 放入物品

**接口路径**: `POST /v1/items/{itemId}/put-in-container`

**接口描述**: 将物品放入指定容器

**路径参数**:
- `itemId`: 物品ID

**查询参数**:
- `containerId`: 容器ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success"
}
```

---

### 5.16 统计容器中的物品数量

**接口路径**: `GET /v1/items/count/container/{containerId}`

**接口描述**: 获取指定容器中的物品数量

**路径参数**:
- `containerId`: 容器ID

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": 10
}
```

---

### 5.17 统计各状态物品数量

**接口路径**: `GET /v1/items/count/status`

**接口描述**: 获取各状态物品的数量统计

**请求参数**: 无

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "active": 50,
    "expired": 10,
    "archived": 5
  }
}
```

---

### 5.18 统计各分类物品数量

**接口路径**: `GET /v1/items/count/category`

**接口描述**: 获取各分类物品的数量统计

**请求参数**: 无

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "食品": 30,
    "日用品": 20,
    "电子产品": 15
  }
}
```

---

## 6. 存储管理

### 6.1 图片上传

**接口路径**: `POST /api/storage/uploadImg`

**接口描述**: 上传图片到指定的存储服务

**请求体**: `ImageBase64UploadRequestDto`
```json
{
  "base64": "string",
  "fileName": "string",
  "fileType": "string"
}
```

**返回格式**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "url": "string",
    "fileName": "string"
  }
}
```

---

## 通用说明

### 认证方式

大部分接口需要在请求头中携带认证信息:

```
Authorization: Bearer {token}
```

### 通用响应格式

所有接口的响应格式统一为:

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

- `code`: 响应状态码,200表示成功
- `message`: 响应消息
- `data`: 响应数据

### 错误码说明

常见错误码:
- `200`: 成功
- `400`: 请求参数错误
- `401`: 未授权
- `403`: 无权限
- `404`: 资源不存在
- `500`: 服务器内部错误

---

## 更新日志

- 2024-11-02: 初始版本

